{
  "name": "educontrol",
  "version": "1.0.0",
  "private": true,
  "description": "Gestión de cursos, estudiantes y profesores (Angular + Express + MongoDB)",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "serve": "npm run build && npm start",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm start --prefix frontend",
    "test": "npm test --prefix backend",
    "test:web": "npm test --prefix frontend -- --watch=false --browsers=ChromeHeadless",
    "test:web:cov": "npm run test:cov --prefix frontend",
    "test:e2e": "playwright test",
    "capturas": "node scripts/capturas.mjs",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "lint:fix": "npm run lint:fix --prefix backend && npm run lint:fix --prefix frontend",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "prettier": "^3.9.6"
  }
}
